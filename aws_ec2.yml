# aws_ec2.yml
plugin: amazon.aws.aws_ec2
regions:
  - us-east-1  # Update to your AWS region
filters:
  instance-state-name: running  # Only target running instances
keyed_groups:
  - key: tags.Name
    prefix: instance
